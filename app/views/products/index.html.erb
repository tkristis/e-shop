<% if signed_in? && current_user.admin? %>
<%= link_to 'New Product', new_product_path %>
<% end %>

<h1 id="listing-products">Listing products</h1>

<div class="container-fluid">
  <div class="row">
    <% cache(cache_key_for_products) do %>
    <%= render @products %>
    <% end %>
  </div>
</div>

<br>

